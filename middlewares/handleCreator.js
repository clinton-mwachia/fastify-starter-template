//const mongoose = require("mongoose");
//const User = require("../models/user");

// a middleware to check if the user is getting the todo they created
const isCreatorSingleTodo = async (request, reply) => {
  // TODO
};

module.exports = isCreatorSingleTodo;
